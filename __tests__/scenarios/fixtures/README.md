# Scenario fixtures

Small reusable test applications that the scenario suites spawn under
different runtimes (Node, Bun) and in different module systems (CJS,
ESM). Each fixture directory groups a small set of related files that
together exercise one specific scenario shape.

See [`files.md`](./files.md) for the per-subdirectory inventory.
